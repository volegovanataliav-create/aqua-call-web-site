# Решение проблем: Публичный сайт выглядит не так, как локальный

## 🔍 Быстрая диагностика

### 1. Где размещен ваш сайт?

**Проверьте URL:**
- `https://username.github.io/aqua-call-web-site/` → **GitHub Pages** (нужен base path)
- `https://project.vercel.app` → **Vercel** (base path не нужен)
- `https://project.netlify.app` → **Netlify** (base path не нужен)

### 2. Основные причины различий

#### ❌ Проблема: Неправильный base path (GitHub Pages)

**Симптомы:**
- Ссылки не работают
- Изображения не загружаются
- CSS/JS файлы не загружаются (404 ошибки)

**Решение:**
Откройте `.vitepress/config.mts` и убедитесь, что для GitHub Pages указан base:

```typescript
base: '/aqua-call-web-site/',
```

Для Vercel/Netlify оставьте:
```typescript
base: '/',
```

#### ❌ Проблема: Tailwind CSS не работает

**Симптомы:**
- Стили не применяются
- Сайт выглядит без стилей

**Решение:**
1. Убедитесь, что файлы существуют:
   - `.vitepress/theme/tailwind.css` (содержит `@import "tailwindcss";`)
   - `.vitepress/theme/index.ts` (импортирует `'./tailwind.css'`)

2. Пересоберите проект:
   ```bash
   npm run docs:build
   ```

3. Проверьте локально:
   ```bash
   npm run docs:preview
   ```

#### ❌ Проблема: Статические файлы не загружаются

**Симптомы:**
- Логотип не отображается
- Изображения не загружаются

**Решение:**
1. Убедитесь, что файлы в папке `public/`
2. Используйте абсолютные пути: `/logo.svg` (не `./logo.svg`)
3. Если на GitHub Pages, учитывайте base path

## 🛠️ Пошаговое исправление

### Шаг 1: Определите платформу

```bash
# Проверьте URL вашего сайта
```

### Шаг 2: Настройте base path

**Для GitHub Pages:**
```typescript
// .vitepress/config.mts
base: '/aqua-call-web-site/',
```

**Для Vercel/Netlify:**
```typescript
// .vitepress/config.mts
base: '/',
```

### Шаг 3: Пересоберите и задеплойте

```bash
npm run docs:build
git add .
git commit -m "Fix production build"
git push
```

### Шаг 4: Проверьте результат

1. Откройте сайт в браузере
2. Нажмите F12 → Console (проверьте ошибки)
3. Нажмите F12 → Network (проверьте, какие файлы не загружаются)

## 📝 Текущая конфигурация

Конфигурация автоматически определяет окружение:
- GitHub Pages → base: `/aqua-call-web-site/`
- Другие платформы → base: `/`

Если автоматическое определение не работает, вручную установите base в `.vitepress/config.mts`.

## 🆘 Если ничего не помогло

1. Проверьте консоль браузера (F12) на ошибки
2. Проверьте Network tab - какие файлы возвращают 404
3. Сравните локальную сборку (`npm run docs:preview`) с production
4. Убедитесь, что все зависимости установлены: `npm install`

